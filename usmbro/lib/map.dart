import 'package:flutter/material.dart';

class MapUsers extends StatelessWidget {
  const MapUsers({Key? key, required String title}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Map'),
      ),
    );
  }
}
